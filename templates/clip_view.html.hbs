<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/svg+xml" href="/assets/frontend/vite.svg" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Clip View</title>
    <style>
        * {
            box-sizing: border-box;
        }

        html, body {
            margin: 0;
            padding: 0;
            font-family: -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Oxygen, Ubuntu, Cantarell, sans-serif;
            background-color: #000;
            height: 100vh;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        .clip-container {
            max-width: 100%;
            max-height: 100%;
            display: flex;
            justify-content: center;
            align-items: center;
        }

        video, img, audio {
            max-width: 100%;
            max-height: 100vh;
            display: block;
        }

        audio {
            width: 400px;
        }
    </style>
</head>
<body>
    <div class="clip-container">
        {{#if is_video}}
            <video src="{{clip_url}}" controls autoplay preload="auto"></video>
        {{/if}}
        {{#if is_gif}}
            <img src="{{clip_url}}" alt="Clip">
        {{/if}}
        {{#if is_audio}}
            <audio src="{{clip_url}}" controls autoplay preload="auto"></audio>
        {{/if}}
    </div>
</body>
</html>
